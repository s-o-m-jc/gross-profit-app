# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Application Specific Vite Variables
#
# 3社（大阪人材・四国人材・松山人材）は1つのアプリ内で会社を切り替えて運用する方針
# (以前は会社ごとに別インスタンス/別デプロイだったが統合済み)。
# 会社名・決算開始月は会社ごとに切り替える値のため、環境変数ではなく
# src/config/companies.ts の設定テーブルで管理する(そちらを編集すること)。
# ここに置くのは、3社共通で1つのデプロイ全体に適用される値のみ。
VITE_DEFAULT_TAX_RATE=0.1
VITE_LOW_MARGIN_THRESHOLD=10

# Supabase (認証 + データベース)。実際の値は Supabase ダッシュボードの
# Project Settings > API から取得し、.env.local (Gitにコミットしない) に設定すること。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
